import {
  Connector
} from "./chunk-BNISQZGL.js";
import {
  init_define_process_env
} from "./chunk-PLO4ZEQ3.js";

// dep:@snapshot-labs_lock_connectors_kaikas
init_define_process_env();

// node_modules/@snapshot-labs/lock/connectors/kaikas.ts
init_define_process_env();
var Connector2 = class extends Connector {
  async connect() {
    let provider;
    if (window["klaytn"]) {
      provider = window["klaytn"];
      try {
        await window["klaytn"].enable();
      } catch (e) {
        console.error(e);
        if (e.code === -32603)
          return;
      }
    } else if (window["caver"]) {
      provider = window["caver"].currentProvider;
    }
    return provider;
  }
  async isLoggedIn() {
    if (!window["klaytn"])
      return false;
    if (window["klaytn"].selectedAddress)
      return true;
    await new Promise((r) => setTimeout(r, 400));
    return !!window["klaytn"].selectedAddress;
  }
};

// dep:@snapshot-labs_lock_connectors_kaikas
var snapshot_labs_lock_connectors_kaikas_default = Connector2;
export {
  snapshot_labs_lock_connectors_kaikas_default as default
};
//# sourceMappingURL=@snapshot-labs_lock_connectors_kaikas.js.map
